{
  "name": "pi-model-images",
  "version": "0.1.0",
  "description": "Image-generation support for pi: a codex-style imagegen tool that calls the OpenAI Images API directly (works with any provider), plus transparent stream wrapping (responses/completions protocols), generic data-URI catching, and inline kitty/iterm2 image display.",
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions/model-images.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
